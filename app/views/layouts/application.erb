<!DOCTYPE html>
<html>
  <head>
    <title>AmazingEvents</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag    "vendor/bootstrap.min", media: "all" %>
    <%= javascript_include_tag "jquery", "jquery-ujs", "application" %>
    <%= javascript_include_tag "vendor/bootstrap.min" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="navbar navbar-default" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'AmazingEvents', url(:welcome, :index), class: 'navbar-brand' %>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to 'イベントを作る', url(:events, :new) %></li>
            <% if logged_in? %>
              <li><%= link_to '退会', url(:users, :retire) %></li>
              <li><%= link_to 'ログアウト', url(:sessions, :destroy) %></li>
            <% else %>
              <li><%= link_to 'Twitterでログイン', '/auth/twitter' %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </header>
    <div class="container">
      <% if flash[:notice] %>
        <div class="alert alert-success">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="alert alert-danger">
          <%= flash[:alert] %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
